/**
 * Capability provenance is one setting shared by the app and the co-hosted
 * agent; both read the same environment in the same Vercel project.
 */
export { liveEvidenceEnabled, modelMode, type ModelMode } from '../../src/assessment-http/settings';
